#ifndef EASYFIND_HPP
# define EASYFIND_HPP

#include <iostream>
#include <vector>

template <typename T>
void easyfind(T arr, int b)
{
	//std::vector<int> arr;
	auto iterFound = find(arr.begin(), arr.end(), b); 

}

#endif
